name: Deploy Theme to cPanel via SSH

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy Theme
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Trigger Webhook Deployment
        run: curl -s "http://buyinlatam.com/deploy.php?token=latam_deploy_2026_secreto"
